# Worklog — Levit AI 직무과제: 분류·속성 파이프라인 설계 → 웹/PDF 배포 · 2026-06-29

## 한 일 (What we did)

"500만 상품 × 1만 카테고리" 규모의 두 AI 파이프라인(카테고리 분류 / 속성 추출)을 **설계 문서**로 만들고, 한국형 트렌디 웹 + PDF로 빌드해 GitHub Pages에 배포했다. 코드 구현 과제가 아니라 *설계와 그 근거*가 산출물이라, 작업의 무게중심은 (1) 무엇을 증명할지 정하기, (2) 시간민감 사실을 검증해 방어 가능한 설계를 쓰기, (3) 평가자가 읽기 좋게 시각화하기 — 셋이었다. 초기 전체 산출물이 커밋 `3994440`, 이후 라이트 디폴트 전환이 `0d52510`. 두 커밋 사이의 진짜 이야기는 git log가 담지 못하는 **결정의 이유와 반복 피드백**이다.

## 결정과 왜 (Decisions & why)

| 결정 | 버린 대안 | 왜 |
|------|----------|----|
| `/deep-interview`로 요구사항부터 고정 | 바로 설계 착수 | 코드가 아닌 설계 과제라 "무엇을 증명하나(포지셔닝)"가 모든 깊이 결정을 좌우. 모호도 100%→13%로 내린 뒤 시작 |
| 포지셔닝 = 프로덕션 실용성·트레이드오프 논증·비용감각(시니어 IC) | 연구/혁신 과시, 제품/임팩트 | 제출 후 "기술 심화 면접"이 명시 → "왜 이 선택인가"를 방어하는 설계가 가장 강함 |
| 분류 = "Distilled Funnel"(임베딩→ANN→계층 rerank→신뢰도 routing) | 단일 거대 분류기 / 순수 LLM 분류 | 1만 클래스는 flat도 가능하나 cold-start·롱테일·계층일관성에 약함. 깔때기는 단계별 독립 튜닝 + 하드케이스만 비싼 모델 |
| **비용은 binding 제약이 아니다**를 핵심 논점으로 | 비용 최소화를 전면에 | 검증된 단가로 계산하니 self-host 하이브리드 전수처리 ~$1–5K(예산의 5–7%). naive 전수 LLM은 $75–150K로 초과 → "어디에 비싼 걸 쓰나"가 설계의 본체 |
| 정답 데이터 = LLM 교사 라벨링 → distillation | 사람 전수 라벨 / 라벨 없이 학습 | 분류 모델을 새로 만든다 = 정답 leaf가 없다. 이 공백을 푸는 방식이 설계 성패. 비싼 모델은 가르치는 데 한 번만 |
| 포괄성(CE) 측정 = 검색로그 gap 마이닝 | golden recall만 | "빠짐없음"은 모르는 걸 재야 하는 열린 문제. 실서비스(올웨이즈)에선 "원했는데 못 채운 검색"이 측정 가능한 누락 신호 |
| 단일 마크다운 → 웹+PDF 커스텀 정적 빌드 | SSG(Astro/MkDocs), Mermaid | Linear/Stripe-docs 미감 + PDF 견고함을 위해 풀 컨트롤. 다이어그램도 HTML/CSS로(테마·PDF·no-JS 모두 견고, Mermaid는 PDF 렌더 불안정) |
| 인용은 검증 후 references로 | 본문에 시스템명 나열 | 1차 검증 결과 BEATS·AutoPKG·MADIAVE는 실재(venue 교정), macro-F1 비교치는 출처불명 → 후자는 제거. 본문은 명명 없이도 성립하게 |
| 라이트 디폴트(다크는 토글) | OS `prefers-color-scheme` 추종 | 평가자 첫인상을 일관되게. 사용자가 고른 다크만 저장·유지 |

## 배운 것 (Learned)

- **`color-scheme`는 스크롤바뿐 아니라 인쇄 캔버스 배경을 결정한다.** 스크롤바 라이트화로 넣은 `color-scheme`가 헤드리스(OS 다크)에서 PDF 페이지 여백을 검게 만든 회귀 → `@media print { color-scheme: light }`로 차단. (화면 fix가 인쇄에 샌 사례)
- **점진적 향상 애니메이션은 기본이 "보이는" 상태여야 한다.** reveal을 `opacity:0`에서 시작했더니 PDF/no-JS에서 사라짐 → 인라인 width + CSS 키프레임(0→값), print는 강제 가시화.
- **모바일 브레이크포인트는 인쇄에서도 켜진다.** A4(~794px) < 980px라 PDF에서 카드가 1열로 쌓임(가독상 허용).
- **시간민감 사실(LLM 가격·모델·SOTA)은 기억이 아니라 웹 1차 소스로.** cutoff 이후 드리프트가 커서 6개 병렬 에이전트로 검증·인용.
- **과한 인용 보수화는 진짜 인용을 지운다.** 미래시점 arxiv ID를 할루시네이션으로 단정해 실재 시스템을 뺐다가, 검증 후 복원. 의심 시 "지우기" 전에 "검증"이 먼저.

## 세션 흐름 (How it moved)

1. `/deep-interview` 6라운드 → 스펙 확정(포지셔닝·산출물·기술 stance).
2. 6개 병렬 리서치(한국어 모델·API가·GPU경제성·분류SOTA·속성·회사맥락) → `research-synthesis`로 종합.
3. Opus가 설계 7장 작성(00 요약 ~ 06 AI활용).
4. 커스텀 정적 빌드(웹+PDF), puppeteer/pdftoppm로 시각 검증.
5. 사용자 피드백 반복: 시각 다이어그램 5개·강한 도입 요약·de-slop(어려운 용어 정리)·헤더 가림·라이트 스크롤바·**PDF 검은 여백 회귀**·라이트 디폴트.
6. givepro91 계정으로 리포 생성·push·Pages 활성화 → 라이브 확인 → jay-swk 복원.

## 남긴 것 (Left open)

- **차단 없음.** 선택적 후속: 더 깊은 de-slop, 커스텀 도메인 연결, 검증된 인용을 본문에 일부 복원, MAVE 저자소속 같은 PARTIAL 교정의 인라인 반영.
- `.omc/`(deep-interview 스펙·리서치 종합)는 의도적으로 리포에서 제외(작업 노트). 과정 서사는 6장에 정제본으로 존재.

## 결과·검증 (Outcome)

- **배포됨:** public 리포 `givepro91/levit-ai`, 라이브 `https://givepro91.github.io/levit-ai/`(HTTP 200), PDF `assignment.pdf`(36p).
- **검증(수동):** 웹은 puppeteer 스크린샷으로 히어로·5개 다이어그램·비용차트·모바일·**라이브 공개 URL**까지 확인. PDF는 pdftoppm 페이지 렌더로 라이트 배경·다이어그램·비용차트·클리핑 없음 확인. 라이브는 curl 200 + DOM 요소(다이어그램 5·Pretendard·PDF 링크) 확인.
- **자동 검증 없음:** `/gate` 미실행이라 `.nova/gate-verdict.json` 없음 → 본 기록의 "검증"은 전부 수동 도구(puppeteer·pdftoppm·curl) 기반임을 명시.
